# @package _global_
# L_cls + L_vis
defaults:
  - override /trainer: gpu

tags: ["ablation", "cls_vis"]

model:
  use_teacher: true
  use_img_kd: true
  use_txt_kd: false

logger:
  wandb:
    tags: ${tags}
    name: "${data.data_name}_ablation_cls_vis"
